# StaleZero vs Workflow Engines

Workflow engines orchestrate long-running business processes. StaleZero is narrower.

StaleZero coordinates immediate cache, view, state, queue, and notification consequences after a mutation. It is not a durable workflow runtime, saga engine, or distributed transaction coordinator.
